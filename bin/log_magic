#!/usr/bin/env ruby

require 'log_magic'

explainer_server_job = fork do
  exec 'start_explainer_server'
end
Process.detach(explainer_server_job)

puts ARGV[0]
log_magic = LogMagic.new(ARGV[0])
log_magic.initialize_listener
log_magic.start
